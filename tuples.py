# to create a tuple use (), tuples can't be edited
coordinates = (4, 5)
# to access a tuples used []
print(coordinates[0])

# tuples inside a list, can't be changed
points = [(4, 5), (6, 7), (80, 34)]



